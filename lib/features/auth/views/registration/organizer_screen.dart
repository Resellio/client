import 'package:flutter/widgets.dart';

class OrganizerRegistrationScreen extends StatelessWidget {
  const OrganizerRegistrationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
